# RFC 0006: DeepEP Dispatcher Adapter

## Status

Placeholder for Phase 4.

DeepEP consumes `topk_idx` and `topk_weights` and returns dispatch/combine handles. RouteForge should treat DeepEP as a DispatcherAdapter for R3/R4, not as the core ABI.
